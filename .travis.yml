language : go

os:
- linux

go:
- 1.11.x
- tip

go_import_path: go.bmvs.io/orderedmap

sudo: false

before_install:
- go get -u github.com/mattn/goveralls

script:
- go test -v -race ./...

after_success:
- goveralls -service=travis-ci

notifications:
  email:
    on_success: change
    on_failure: always
